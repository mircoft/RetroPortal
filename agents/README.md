# RetroPortal agents

Install in development mode:

```bash
cd agents
pip install -e ".[dev]"
```

Optional browser fetcher (permitted hosts only):

```bash
pip install -e ".[playwright]"
playwright install chromium
```
